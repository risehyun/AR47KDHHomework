// 003_MainFunction.cpp : 이 파일에는 'main' 함수가 포함됩니다. 거기서 프로그램 실행이 시작되고 종료됩니다.
//

#include <iostream>


// 프로그램을 실행할거야 -> 이걸 진입점이라고 한다.
// 이 진입점을 기준으로 실행흐름이 생긴다.
// 이러한 실행 흐름을 함수라고 부르기로 했다.

int main() // <= int main() 이것이 바로 프로그램의 진입점이다.
{
  // 함수의 시작

    std::cout << "Hello World!\n";

  // 함수의 끝
}
// 이러이러할 거다.

/*

프로그램(설계도) -> 
프로세스(건물 터를 잡아야 해, 기둥을 세워야 해->건물터가 생김. 설계도보다 용량이 커짐) 
-> CPU (진짜 기둥을 세우는 일을 함, 용량이 더 커짐)

*/